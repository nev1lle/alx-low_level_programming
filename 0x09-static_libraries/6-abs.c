#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the integer to check
 * Return: 0 if successful
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	return (0);
}

