#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: the 1st integer to multiply
 * @b: the 2nd integer to multiply
 * Return: always 0
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (0);
}
