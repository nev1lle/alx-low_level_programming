#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: the 1st integer to multiply
 * @b: the 2nd integer to multiply
 * Return: result of operation
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
